# github-sync secret/PII scan allowlist (EXAMPLE)
#
# Copy this to  secret-scan-allow.txt  (gitignored, per-user) to suppress
# CONFIRMED false positives only. One Python regex per line; # = comment.
#
# A finding is suppressed if a rule matches the matched text OR "relpath:lineno".
# NEVER add a rule that would hide a real secret just to make a push succeed.
#
# Examples:
#
# Allow a specific public contact email you intentionally publish:
# contact@mycompany\.com
#
# Allow everything flagged on one known-safe line:
# docs/example\.md:42
#
# Allow a documented fake key used in a tutorial file:
# tutorials/auth\.md:.*
